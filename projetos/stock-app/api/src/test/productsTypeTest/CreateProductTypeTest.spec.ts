import { productsTypeRepository } from "../../repositories/productType.repository";
import productTypeService from "../../services/productType.service";

describe("Create product type", () => {
    it("should be able to create a product type", async  () => {
        // const productTypeRepository  = new productsTypeRepository();
        // const productTypeService = new productTypeService(productTypeRepository);
        // const productType = await 
        expect(2+2).toBe(4);
    })
})